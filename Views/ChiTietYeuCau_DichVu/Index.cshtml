@model IEnumerable<NhaKhoaMVC5.Models.ChiTietYeuCau_DichVu>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Thêm mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayName("Mã yêu cầu")
        </th>
        <th>
            @Html.DisplayName("Tên dịch vụ")
        </th>
        <th>
            @Html.DisplayName("Tên nha sĩ")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.YeuCau.MaYC_ID)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.DichVu.TenDV)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.YeuCau.MaNhaSi)
    </td>
    <td>
        @Html.ActionLink("Sửa", "Edit", new { id = item.Stt_ID }) |
        @Html.ActionLink("Chi tiết", "Details", new { id = item.Stt_ID }) |
        @Html.ActionLink("Xóa", "Delete", new { id = item.Stt_ID })
    </td>
</tr>
}

</table>
