@model IEnumerable<NhaKhoaMVC5.Models.ChucVu>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenCV)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenCV)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaCV_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaCV_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaCV_ID })
        </td>
    </tr>
}

</table>
