@model IEnumerable<NhaKhoaMVC5.Models.DichVu>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenDV)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Gia)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenDV)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Gia)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaDV_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaDV_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaDV_ID })
        </td>
    </tr>
}

</table>
